---
'@backstage/connections': patch
---

Added a `title` field to connection auth methods, providing a human-readable display name for each configured authentication option. When not explicitly configured, the title defaults to the auth method display name defined by the connection type.
